# src/ — planned frontend module layout

No application code exists yet — this tree is a pre-implementation scaffold reflecting the module boundaries the ADRs already imply (each subfolder's own `README.md` names its ADR). Built on npm + Vite (ADR-0012). See `agents.md`'s "Planned Frontend Module Layout" table for the same map in one place.
