# dialog

Promise-based alert/confirm/prompt modal system, to be adopted here the same way as `OrgVisualizr/js/dialog.js` — no native `confirm()`/`alert()` — once any canvas (Object/Backlog) needs a confirmation prompt.

Sibling-project convention, see `agents.md`.
