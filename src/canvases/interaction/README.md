# canvases/interaction

Interaction Canvas: draw.io / diagrams.net embed, persisted as mxGraph XML (`.drawio`). Shares its embed integration approach with `canvases/system` (same tool, same ADR) but is a separate module/view per the shell's switcher (ADR-0008).

ADR-0003.
