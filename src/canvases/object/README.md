# canvases/object

Object Canvas: Mermaid diagram source, text+preview editing model (not drag-and-drop). Plain-text format makes this the one canvas where real-time collaborative editing is comparatively cheap (CRDT/OT on a text field) if/when pursued — see ADR-0011's Yjs note.

ADR-0004.
