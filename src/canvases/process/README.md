# canvases/process

Process Canvas: bpmn-js integration, persisted as BPMN 2.0 XML (`.bpmn`). The only canvas with a docked element-properties panel (`@bpmn-io/properties-panel`, constrained to a subset matching ADR-0002's BPMN profile), shown only in this single-canvas view.

ADR-0002, ADR-0007 (properties panel).
